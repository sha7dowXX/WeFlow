interface:
  display_name: "WeFlow"
  short_description: "Repo-specific patterns and workflows for WeFlow"
  default_prompt: "Use the WeFlow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true